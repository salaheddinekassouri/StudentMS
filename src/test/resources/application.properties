# ================================
# = DATASOURCE (PostgreSQL - Test)
# ================================
spring.datasource.url=jdbc:postgresql://dpg-d4mvdqbe5dus738msd4g-a:5432/studentms_db_hbhg
spring.datasource.username=studentms_db_hbhg_user
spring.datasource.password=qWttSwizHR27VEWYJwiHWVcZ06ULgOmO
spring.datasource.driver-class-name=org.postgresql.Driver

# ================================
# = JPA / HIBERNATE
# ================================
# Create schema for each test run and drop after
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Disable SQL formatting (tests run faster)
spring.jpa.properties.hibernate.format_sql=false
server.port=8084
# ================================
# = TEST ONLY CONFIG
# ================================
# Disable web server for unit tests
spring.main.web-application-type=none

# Disable actuator entirely in test mode
management.endpoints.enabled-by-default=false
